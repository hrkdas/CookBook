<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dasshrkcodes.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CookBook">
        <activity android:name=".recipe_screen_activities.image_overview"></activity>
        <activity android:name=".searchby_cardtype" />
        <activity android:name=".search_screen" />
        <activity android:name=".profiles_n_wishlist_page" />
        <activity android:name=".login_screen" />
        <activity android:name=".app_info" />
        <activity android:name=".recipe_screen_activities.start_cooking_screen" />
        <activity android:name=".main_screen" />
        <activity android:name=".recipe_screen_activities.recipe_overview" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>